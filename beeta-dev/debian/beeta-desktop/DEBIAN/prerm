#!/bin/bash
# Beeta Desktop — Pre-removal script
set -e

case "$1" in
    remove|purge)
        # Clean up cache files
        rm -rf /var/cache/beeta-theme 2>/dev/null || true
        ;;
esac


exit 0
